<div class="container">
	<section class="alert alert-warning mrgn-tp-lg">
		<h2>{{ alerts[locale].alertHeading }}</h2>
			{% if locale == "en" %}
			This site is currently a proof of concept, and some of the content may not be up to date. We are continuously working on improving the site's features and accuracy. Your understanding is appreciated as we refine this project.
			{% else %}
			Ce site est actuellement une preuve de concept, et certains contenus peuvent ne pas être à jour. Nous travaillons continuellement à l'amélioration des fonctionnalités et de la précision du site. Nous vous remercions de votre compréhension pendant que nous peaufinons ce projet.
			{% endif %}
		<ul>
			{% if isDraft == true %}
			<li class="mrgn-tp-lg">{{ alerts[locale].draftText }}</li>
			{% endif %}
			{% if needsTranslation == true %}
			<li class="mrgn-tp-lg" lang="{{ otherLang }}">{{ alerts[locale].translationText }}</li>
			{% endif %}
			{% if internalLinks == true %}
			<li class="mrgn-tp-lg">{{ alerts[locale].internalLinkText }}<i class="fas fa-external-link-square-alt" aria-hidden="true"> <span class="mrgn-lft-sm">{{ alerts[locale].icon }}</span cl></i><span class="wb-inv">{{ alerts[locale].hiddenTextLink }}</span></li>
			{% endif %}
		</ul>
	</section>
</div>
